import 'package:flutter/material.dart';

class CloseButtonWidget2 extends StatelessWidget {
  const CloseButtonWidget2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'CloseButtonWidget2 is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}